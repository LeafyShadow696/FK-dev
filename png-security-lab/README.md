# PNG Security Lab PWA

Cross-device browser/PWA PNG parser regression lab.

Flow: Browser → PWA → Drag & Drop / Select PNG → WebAssembly → local results.

The project is intentionally limited to safe parser validation and regression testing. It contains no shellcode, exploit primitive, or automatic code execution.
