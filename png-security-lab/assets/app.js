const $=id=>document.getElementById(id);
const corpus=["01_boundary_max.png","02_integer_overflow_candidate.png","03_invalid_ihdr_crc.png","04_bad_signature.png","05_truncated_giant_text.png","06_zero_width.png","07_zero_height.png","08_invalid_color_type.png"];
const descriptions={
"01_boundary_max.png":"Maximum PNG width boundary; header-only fixture.",
"02_integer_overflow_candidate.png":"Arithmetic stress case for width × height × RGB byte calculations.",
"03_invalid_ihdr_crc.png":"IHDR CRC intentionally corrupted.",
"04_bad_signature.png":"PNG signature intentionally corrupted.",
"05_truncated_giant_text.png":"Chunk declares a huge length but the input is truncated.",
"06_zero_width.png":"Zero-width dimension boundary.",
"07_zero_height.png":"Zero-height dimension boundary.",
"08_invalid_color_type.png":"Invalid IHDR color type boundary."
};
const map={0:"accepted",1:"bad signature",2:"too short",3:"invalid IHDR",4:"CRC mismatch",5:"truncated chunk",6:"invalid dimensions/color","-1":"runtime error"};
let wasm=null,deferred=null,last=[],selectedBytes=null,editedBytes=null,selectedFileName="";
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const hexByte=n=>n.toString(16).padStart(2,'0').toUpperCase();
const hex32=n=>(n>>>0).toString(16).padStart(8,'0').toUpperCase();
const hexDump=(bytes,max=2048)=>{const n=Math.min(bytes.length,max),lines=[];for(let i=0;i<n;i+=16){const row=bytes.slice(i,Math.min(i+16,n));const h=[...row].map(hexByte).join(' ').padEnd(47,' ');const a=[...row].map(x=>(x>=32&&x<127)?String.fromCharCode(x):'.').join('');lines.push(i.toString(16).padStart(8,'0').toUpperCase()+"  "+h+"  "+a)}if(bytes.length>max)lines.push(`… ${bytes.length-max} more bytes`);return lines.join('\n')};
function crc32(bytes){let t=crc32.table;if(!t){t=[];for(let n=0;n<256;n++){let c=n;for(let k=0;k<8;k++)c=(c&1)?(0xedb88320^(c>>>1)):(c>>>1);t[n]=c>>>0}crc32.table=t}let c=0xffffffff;for(const b of bytes)c=t[(c^b)&255]^(c>>>8);return(c^0xffffffff)>>>0}
function read32(b,p){return((b[p]<<24)|(b[p+1]<<16)|(b[p+2]<<8)|b[p+3])>>>0}
function write32(b,p,n){b[p]=(n>>>24)&255;b[p+1]=(n>>>16)&255;b[p+2]=(n>>>8)&255;b[p+3]=n&255}
function inspectPNG(bytes){const rows=[];const sig=[0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a];const validSig=bytes.length>=8&&sig.every((v,i)=>bytes[i]===v);let pos=8;if(!validSig){rows.push({offset:0,type:"SIGNATURE",length:8,crc:"—",state:"BAD"});return{validSig,rows}}while(pos+12<=bytes.length){const len=read32(bytes,pos),typeBytes=bytes.slice(pos+4,pos+8),type=new TextDecoder().decode(typeBytes),dataEnd=pos+8+len,crcPos=dataEnd,stored=crcPos+4<=bytes.length?read32(bytes,crcPos):null;let state="OK";if(dataEnd+4>bytes.length)state="TRUNCATED";else if(crc32(bytes.slice(pos+4,dataEnd))!==stored)state="CRC BAD";rows.push({offset:pos,type,length:len,crc:stored===null?"—":hex32(stored),state});if(type==="IEND"||dataEnd+4>bytes.length)break;pos=dataEnd+4}return{validSig,rows}}
async function sha256(bytes){const h=await crypto.subtle.digest("SHA-256",bytes);return[...new Uint8Array(h)].map(hexByte).join("").toLowerCase()}
function parseHex(text){const cleaned=text.replace(/0x/gi,"").replace(/[^0-9a-f]/gi,"");if(cleaned.length===0||cleaned.length%2)return null;const out=new Uint8Array(cleaned.length/2);for(let i=0;i<out.length;i++)out[i]=parseInt(cleaned.slice(i*2,i*2+2),16);return out}
function parseOffset(text){const s=text.trim().toLowerCase().replace(/^0x/,"");if(!/^[0-9a-f]+$/.test(s))return null;return parseInt(s,16)}
function findIHDR(b){if(b.length<33||!(b[12]===0x49&&b[13]===0x48&&b[14]===0x44&&b[15]===0x52))return null;return 8}
function updateEditor(){if(!editedBytes){$("editHex").textContent="Select a PNG first.";$("editHash").textContent="—";return}$("editHex").textContent=hexDump(editedBytes);sha256(editedBytes).then(h=>$("editHash").textContent=h);$("editStatus").textContent=`${editedBytes.length} B · ${selectedFileName} · edits are local until export`}
async function inspectFile(name,b){selectedBytes=b;editedBytes=new Uint8Array(b);selectedFileName=name;$("selectedName").textContent=`${name} · ${b.length} B`;$('hex').textContent=hexDump(b);const a=inspectPNG(b);$('chunkSummary').textContent=`${a.rows.length} chunk record(s)`;$('chunks').innerHTML=a.rows.map(r=>`<tr><td>0x${r.offset.toString(16).padStart(8,'0').toUpperCase()}</td><td><b>${esc(r.type)}</b></td><td>${r.length}</td><td>${r.crc}</td><td class="${r.state==='OK'?'ok':r.state==='CRC BAD'?'bad':'warn'}">${r.state}</td></tr>`).join('');const ih=findIHDR(b);if(ih!==null){$('ihdrWidth').value=read32(b,ih+8);$('ihdrHeight').value=read32(b,ih+12);$('ihdrDepth').value=b[ih+16];$('ihdrColor').value=b[ih+17]}updateEditor()}
async function scan(name,b){const t=performance.now();try{const mem=wasm.instance.exports.memory,ptr=4096;if(ptr+b.length>mem.buffer.byteLength)throw new Error("Input exceeds WASM memory window");new Uint8Array(mem.buffer,ptr,b.length).set(b);const c=wasm.instance.exports.png_scan(ptr,b.length),a=inspectPNG(b);return{file:name,status:c===0?'accepted':'rejected',code:c,width:wasm.instance.exports.png_width()||null,height:wasm.instance.exports.png_height()||null,detail:map[c]||"unknown",chunks:a.rows.length,signature:a.validSig,sha256:await sha256(b),bytes:b.length,ms:performance.now()-t}}catch(e){return{file:name,status:"runtime-error",code:-1,detail:e.message,ms:performance.now()-t}}}
function render(){$("fileCount").textContent=last.length;$('accepted').textContent=last.filter(x=>x.status==='accepted').length;$('rejected').textContent=last.filter(x=>x.status==='rejected').length;$('errors').textContent=last.filter(x=>x.status==='runtime-error').length;$('rows').innerHTML=last.map(r=>{const cls=r.status==='accepted'?'accepted':r.status==='rejected'?'rejected':'error';return`<tr><td>${esc(r.file)}</td><td><span class="status ${cls}">${r.status.toUpperCase()}</span></td><td>${r.width&&r.height?r.width+' × '+r.height:'—'}</td><td>${r.code}: ${esc(r.detail)}</td><td>${r.signature?'signature OK':'signature BAD'} · ${r.sha256?r.sha256.slice(0,12)+'…':'—'}</td><td>${Number(r.ms||0).toFixed(2)} ms</td></tr>`}).join('')}
async function runFiles(list){const files=[...list].filter(x=>x.name.toLowerCase().endsWith('.png'));if(!files.length)return;$('dropStatus').textContent=`Processing ${files.length} PNG file(s)…`;last=[];for(const f of files){const b=new Uint8Array(await f.arrayBuffer());last.push(await scan(f.name,b));await inspectFile(f.name,b)}render();$('dropStatus').textContent=`Processed ${files.length} PNG file(s) locally.`;$('status').textContent=`Completed ${last.length} file(s) · no upload endpoint`}
async function runCorpus(){if(!wasm)return;$('status').textContent='Running built-in corpus…';last=[];for(const n of corpus){const b=new Uint8Array(await(await fetch('corpus/'+n)).arrayBuffer());last.push(await scan(n,b));if(!selectedBytes)await inspectFile(n,b)}render();$('status').textContent=`Corpus complete · ${last.length} fixture(s)`}
function applyPatch(){if(!editedBytes){alert('Select a PNG first.');return}const off=parseOffset($('patchOffset').value),patch=parseHex($('patchBytes').value);if(off===null||!patch){alert('Enter a valid hexadecimal offset and byte sequence.');return}if(off<0||off+patch.length>editedBytes.length){alert('Patch must stay inside the existing file. File size is never expanded.');return}editedBytes.set(patch,off);updateEditor();$('editStatus').textContent+=` · patched 0x${off.toString(16).toUpperCase()}`}
function applyIHDR(){if(!editedBytes){alert('Select a PNG first.');return}const p=findIHDR(editedBytes);if(p===null){alert('Selected file has no canonical IHDR at the expected position.');return}const vals=['ihdrWidth','ihdrHeight','ihdrDepth','ihdrColor'].map(id=>Number($(id).value));if(vals.some(n=>!Number.isInteger(n)||n<0)||vals[0]>0xffffffff||vals[1]>0xffffffff||vals[2]>255||vals[3]>255){alert('Invalid IHDR values.');return}write32(editedBytes,p+8,vals[0]>>>0);write32(editedBytes,p+12,vals[1]>>>0);editedBytes[p+16]=vals[2];editedBytes[p+17]=vals[3];const crc=crc32(editedBytes.slice(p+4,p+25));write32(editedBytes,p+25,crc);updateEditor();$('editStatus').textContent='IHDR changed and IHDR CRC recalculated'}
function repairCRCs(){if(!editedBytes){alert('Select a PNG first.');return}const a=inspectPNG(editedBytes);if(!a.validSig){alert('Not a PNG signature; CRC repair was not applied.');return}let changed=0;for(const r of a.rows){if(r.state==='TRUNCATED')continue;const p=r.offset,len=read32(editedBytes,p),crcPos=p+8+len,calc=crc32(editedBytes.slice(p+4,p+8+len));if(crcPos+4<=editedBytes.length){write32(editedBytes,crcPos,calc);changed++}}updateEditor();$('editStatus').textContent=`Recalculated CRC for ${changed} complete chunk(s)`;}
function downloadEdited(){if(!editedBytes){alert('Select a PNG first.');return}const blob=new Blob([editedBytes],{type:'image/png'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=selectedFileName.replace(/\.png$/i,'')+'_edited.png';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
function resetEdit(){if(!selectedBytes){return}editedBytes=new Uint8Array(selectedBytes);inspectFile(selectedFileName,selectedBytes)}
function setPreset(name){if(name==='max'){$('ihdrWidth').value=2147483647;$('ihdrHeight').value=1;$('ihdrDepth').value=8;$('ihdrColor').value=2}if(name==='overflow'){$('ihdrWidth').value=1431655766;$('ihdrHeight').value=8;$('ihdrDepth').value=8;$('ihdrColor').value=2}if(name==='normal'){$('ihdrWidth').value=1;$('ihdrHeight').value=1;$('ihdrDepth').value=8;$('ihdrColor').value=2}}
function nav(){document.querySelectorAll('.nav').forEach(b=>b.onclick=()=>{document.querySelectorAll('.nav').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));$(b.dataset.view+'View').classList.add('active')})}
function pwa(){if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferred=e;$('install').classList.remove('hidden')});$('install').onclick=async()=>{if(!deferred)return;deferred.prompt();await deferred.userChoice;deferred=null;$('install').classList.add('hidden')}}
function inputs(){const d=$('drop'),fi=$('files'),fo=$('folder');$('pick').onclick=()=>fi.click();$('pickFolder').onclick=()=>fo.click();fi.onchange=()=>runFiles(fi.files);fo.onchange=()=>runFiles(fo.files);['dragenter','dragover'].forEach(t=>d.addEventListener(t,e=>{e.preventDefault();d.classList.add('drag')}));['dragleave','drop'].forEach(t=>d.addEventListener(t,e=>{e.preventDefault();d.classList.remove('drag')}));d.ondrop=e=>runFiles(e.dataTransfer.files)}
function corpusCards(){$('corpusCards').innerHTML=corpus.map(n=>`<article class="fixture"><b>${n}</b><p>${esc(descriptions[n])}</p></article>`).join('')}
$('run').onclick=runCorpus;$('clear').onclick=()=>{last=[];render();$('status').textContent='Cleared'};$('export').onclick=()=>{const u=URL.createObjectURL(new Blob([JSON.stringify(last,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=u;a.download='png-security-lab-results.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),500)};$('applyPatch').onclick=applyPatch;$('applyIHDR').onclick=applyIHDR;$('repairCRC').onclick=repairCRCs;$('downloadPng').onclick=downloadEdited;$('resetEdit').onclick=resetEdit;document.querySelectorAll('.preset').forEach(b=>b.onclick=()=>setPreset(b.dataset.preset));
(async()=>{nav();pwa();inputs();corpusCards();try{const b=await(await fetch('wasm/pnglab.wasm')).arrayBuffer();wasm=await WebAssembly.instantiate(b,{});$('status').textContent=`WebAssembly ready · module v${wasm.instance.exports.version()}`;}catch(e){$('status').textContent='WASM load failed: '+e.message}})();